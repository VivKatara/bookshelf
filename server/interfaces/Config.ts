export interface Config {
  port: number;
  databaseUrl: string;
  googleBooksApiKey: string;
  accessTokenSecret: string;
  refreshTokenSecret: string;
  sessionSecret: string;
  googleBooksVolumeApiUrl: string;
}
